## 1. 参赛资格与队伍

1. 面向全球个人与团队开放。每人一个账号。
2. 队伍 1–3 人。每人只能属于一个队伍。提交以队伍名义进行。
3. 主办方、评测平台维护者及其直接合作者可参加Playground，但不参与评奖；其队伍在榜单上标记为隐藏。
4. 队名与内容须遵守行为准则（第 8 节）。

## 2. 阶段

| 阶段 | 日期（UTC） | 提交 | 榜单 |
|---|---|---|---|
| Playground | 注册起至颁奖日 | decisions.csv，每队每天 50 次 | 仅供参考 |
| 线上比赛 | 2026-10-04 16:00 至 2026-10-07 15:59（UTC+8 为 10 月 5–7 日） | decisions.csv，每队每天 10 次 | 决定奖项 |
| 颁奖日 | 2026-10-17，GOSIM 深圳 | 无 | 公布最终结果 |

若与本文不一致，以本文上方的实时阶段配置表为准。

## 3. 提交内容

1. **结果文件。** 列为 `decision_id, slot_id, action, tile_id, program, request_id, reason` 的 `decisions.csv`，由你在本地运行智能体生成，由冻结的评分器即时评分。Playground 和线上比赛都只收这种文件。线上比赛两个场景的天气、预报与天气事件在开赛时公开下载。
2. **不再接受智能体程序包。** 2026 年 9 月 24 日起，各阶段只收结果文件；此前已评分的智能体运行成绩保留在榜上。练习场景仍是赛初的 `participant-agent-protocol-v1` 合约（无异常标签、不能重复观测、不接受上报），异常机制（`participant-agent-protocol-v2`）只在正式比赛场景启用；入门包对两代协议自动兼容，想演练新机制用其中的 `finals-preview` 场景。
3. 文件不超过 20 MB。

## 4. 本地运行

1. 智能体在你自己的电脑上运行，语言、依赖与环境不限，可以联网调用模型 API（用自己的密钥或赞助的兑换码）。
2. 入门包的 `local_runner.py` 按时隙把快照逐个交给智能体，并把它的动作写进 `run_output/decisions.csv`，这就是要提交的文件。
3. 试图读取其他队伍数据、篡改评分器或得分文件、或故意耗尽平台资源，将被取消资格。

## 5. 评分

得分由公开的 `scoring_core.py`（schema `challenge-score-v3`）按 `config/score_config.json` 中的常数计算。概要：

1. 时间是真实夜晚上的 900 秒时隙日历。`observe` 从游标起持续天区的 `nominal_exptime_seconds`，可跨时隙并切分为分段；`wait` 消耗当前时隙剩余时间。
2. 对**完成**曝光的每个分段，在分段中点：
   `A = min(instrument_efficiency · transparency · sky_quality / (seeing_arcsec · airmass), 3.0)`，`combined = A · lunar_quality_factor`，
   `base = V_tile · (segment_seconds / nominal_exptime_seconds) · combined`，其中 `V_tile` 为天区目标的 `science_weight` 之和；
   `bonus = base · B[program]`，`B = {DARK 0.25, BRIGHT 0.15, BACKUP 0.08}`，仅当决策项目等于 `combined` 所对应的区间（DARK ≥ 0.65，BRIGHT ≥ 0.40，否则 BACKUP）时支付。
3. `total = base_science + program_bonus + request_reward + report_reward + coverage_bonus − unsafe_observation − invalid_action − avoidable_wait − required_miss − flexible_shortfall − request_miss − fault_misreport − wrong_tag_report`：每次不安全观测（圆顶关闭时观测）2000；每次无效动作（未知天区/时隙/项目、窗口之外、错误请求标注、在天区落下或夜晚结束前无法完成的曝光、过期决策）100；每秒可避免等待 0.001；每块未完成的必做天区（REQUIRED）1000；每个分区的可选天区（FLEXIBLE）不足 4 块的，每缺一块 100；到期请求每个所需天区 190（完成则每个所需天区奖励 140）。
4. **覆盖均匀性**（`coverage_bonus`）：`coverage_bonus = W · base_science · E`，其中 `E` 是已完成天区在各分区间分布的均匀度（Jain 公平指数：`(Σx)² / (n·Σx²)`，八个分区拍得一样多时为 1，全挤在一个分区时为 1/8）。权重 `W` 写在每个场景的 `config/score_config.json` 里：**练习场景为 0**（不影响你本地跑出的分数），**正式比赛场景为 0.35**。广域巡天要靠均匀覆盖才能支撑统计，所以把天区都集中在好拍的那几个分区会付出代价。
5. **重复观测与异常上报**：重复观测合法——每个天区按历次观测的最高分入账（更差的重复不拉低它），完成状态仍以首次合法观测为准。场景里有隐藏的 per-tile 标签（nova ×1.5、reddening ×0.8，静默作用于评分）和区域级仪器故障（效率骤降、不进预报）。`decision_response` 可附 `reports` 数组上报异常：标签在终局按每 (tile, 标签) 首次上报结算，对 +100、错 −150；故障上报正确则一天后发布故障状态、两天后修复，误报在每次正确上报之间有一次免费额度、之后每次 −100。标签赏罚与故障参数见 `score_config.json` 的 `reporting` / `anomaly_tags` / `fault_response` 小节。

6. 只有完成的曝光计分。被关闭天气中断的曝光不计分也不受罚；被几何或夜晚结束中断的曝光不计分并记为无效动作。
7. 终局惩罚适用于每次运行，包括被时钟或智能体错误截断的运行。可行机会少于所需天区数的到期请求予以免责。
8. 完成度（已完成天区 ÷ 天区数）与各分区可选天区缺额显示在榜单上；它们通过终局惩罚进入得分。
9. 阶段含多个场景时，提交得分为各场景得分的算术平均。只有所有场景都完成评分的提交才计入。

## 6. 排名、同分与核验

1. 每队取该阶段最佳的已评分、未被排除的提交。按得分降序排名；严格同分时先提交者在前。
2. 线上比赛榜单实时更新。主办方可在最后几小时封榜，并在核验后公布最终排名。
3. 确认奖项前，主办方可要求前列队伍提供智能体代码与简短方法说明，并用它重新生成 decisions.csv。无法复现的结果将被移除。
4. 若发现评分器缺陷，主办方可重新评分。评分器或常数的任何变更都会带版本号公告，并适用于该阶段全部提交。线上比赛开始前，当前常数为主办方的临时校准值。

## 7. 奖项

| 奖项 | 奖金 | 名额 |
|---|---|---|
| 一等奖 | $2,000 | 1 |
| 二等奖 | $1,000 | 2 |
| 三等奖 | $500 | 3 |

金额为税前。获奖队伍受邀参加 2026 年 10 月 17 日 GOSIM 深圳颁奖日；领奖不要求到场。

## 8. 行为准则

1. 保持尊重。队名、备注或智能体输出中的骚扰、歧视与辱骂内容不被容忍。
2. 不共享账号，不提交其他队伍的成果，不建立多个队伍以放大每日限额。
3. 发现平台缺陷请报告主办方而非利用。欢迎报告评分器或沙箱问题，并将予以致谢。
4. 主办方关于资格、取消资格与奖项的决定为最终决定。

## 9. 数据与隐私

1. 注册信息（姓名、邮箱、单位、GitHub 账号）仅用于举办赛事与联系获奖者。
2. 上传的结果文件与评分报告在颁奖日后 90 天内保存在平台上，提交队伍与主办方可见。
3. 队名、得分与名次公开。

联系方式：hackathon@gosim.org
